# =============================================================================
# NÃO instale este ficheiro: `pip install -r requirements.txt` aqui dentro.
#
# Este requirements é o upstream original (torch==1.1.0, numpy==1.17.1, …) e
# quebra em Python 3.11+ e com PyTorch 2.x.
#
# Setup oficial no ignia-play:
#   - Na raiz: npm run setup:wav2lip  (clone + checkpoints + wav2lip_inference_requirements.txt)
#   - Ou: pip install -r avatar-ai/wav2lip_inference_requirements.txt
#
# Ambiente único com API + avatar: pip install -r backend/requirements.txt
# =============================================================================
